<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dag.core.repository.TraceMapper">

    <insert id="insert" parameterType="com.dag.core.repository.TraceDO">
        INSERT INTO task_trace (id, task_id, node_id, biz_node_id, event_type, task_state, node_state, detail, created_at)
        VALUES (#{id}, #{taskId}, #{nodeId}, #{bizNodeId}, #{eventType}, #{taskState}, #{nodeState}, #{detail}, NOW())
    </insert>

    <select id="selectByTaskId" resultType="com.dag.core.repository.TraceDO">
        SELECT * FROM task_trace
        WHERE task_id = #{taskId}
        ORDER BY created_at ASC, id ASC
    </select>

    <select id="selectByTaskAndNode" resultType="com.dag.core.repository.TraceDO">
        SELECT * FROM task_trace
        WHERE task_id = #{taskId} AND node_id = #{nodeId}
        ORDER BY created_at ASC, id ASC
    </select>
</mapper>
